(function(){
    // Reserved: central initializer if needed in future
    SiteUtils.onReady(function(){
        // allow manual trigger if needed
        if(window.initTheme) window.initTheme();
    });
})();
